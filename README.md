# Token 消耗排名 - 在线版使用说明

## 使用方法

1. 打开页面
2. 点击 **"📊 上传 CSV"** 按钮，从 DeepSeek 导出的 CSV 文件
3. 页面自动渲染数据，展示排名、图表、趋势等

也支持直接将 CSV 文件拖拽到页面任意位置。

## 部署到 Vercel

1. 将 `index.html` 推送到 GitHub 仓库
2. 在 [Vercel](https://vercel.com) 导入仓库，一键部署
3. 每次更新数据：从 DeepSeek 导出最新 CSV，用户打开页面上传即可
